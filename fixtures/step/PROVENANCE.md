# Where these files came from

Every file here was generated by this repository. Nothing was downloaded,
nothing carries a third party's licence, and all of it may be redistributed
under the same MIT terms as the rest of FerriteCAD.

That is the point of the corpus and also its boundary. A file written by Open
CASCADE and read by Open CASCADE proves a round trip. It does not prove that a
reader copes with what SolidWorks, NX or Creo produce, which is a separate
**interoperability corpus** that does not exist yet and must not be considered
covered by this one.

## What produced them

| | |
|---|---|
| Generator | `tools/build-step-corpus`, at commit `a283b6cef5131115acd7f04915b86b5da83576a5` |
| Open CASCADE | tag `V8_0_1`, commit `b8f597c677811d1f9f4d8a97f5ae2825c0353a42` |
| Source archive SHA-256 | `dba62b81078dd43cec23feba89432be301582341001edad1b93342ad8bda35ea` |
| Platform | Linux, in the pinned-OCCT workflow |
| Header timestamp | fixed at `2020-01-01T00:00:00` |
| STEP schema | AP242 (`AP242_MANAGED_MODEL_BASED_3D_ENGINEERING_MIM_LF`) |

Linux is the canonical platform because it had to be one of them, not because
the others disagree: the semantic manifest is identical on Linux, Windows and
macOS, and six of the seven files are identical byte for byte as well.

## Two different questions, two different answers

**"Has the committed input changed?"** is answered by the checksums below.
They pin these exact bytes, which is what a regression test needs.

**"Can we still produce this model?"** is answered by the semantic manifest,
which the pin workflow regenerates and compares on every run.

These are deliberately not the same question, because Open CASCADE's STEP
writer is not byte-reproducible: it emits colour statements in an order that
varies between runs. `02-flat-assembly.step` differs between two generations
on the same machine, and between macOS and the other two platforms, while
every reader makes exactly the same model of it. A checksum gate on generation
would fail for a reason that has nothing to do with the geometry; see
`docs/build-occt.md`.

## The corpus

| File | Bytes | SHA-256 |
|---|---:|---|
| `01-single-part.step` | 15408 | `fcbc47a2f551f9cec99340e8bcc8f0fdf980dcbd55e7b6a82c680e9ab2dea22a` |
| `02-flat-assembly.step` | 23528 | `aed0cc6f222b882d7d1e95e9edebb326e2f50d52525fc8002a4192d9686f1b39` |
| `03-nested-assembly.step` | 20081 | `e396a9f7527928fa352471415a19fd0c550c8f86299717ce6e6804c22738ee63` |
| `04-instance-colours.step` | 10282 | `0ff09ceb641b239b471eaa28cffd7c132089d820ac9025e378255dcad135b173` |
| `05-inch-units.step` | 15770 | `e895a15c21788882585a4d21fea7d427d7dded4d341951bcb8581dee368c4fb0` |
| `06-unicode-names.step` | 32508 | `3881bc04187496fe8ce7173562a957aa35730520a8848e98044ff269e71ad64a` |
| `07-bare-geometry.step` | 15410 | `c06ce681993ac18c5e084dab6067a770375f2669ccbb5d13e026477095f2865c` |
| `SEMANTIC-MANIFEST.txt` | 8029 | `eed0a155cf5d0a9b4e535c14ebe2dea0a69ca5fb0eb31f63705ff03439533e63` |

## The damaged variants

Derived by `step_corpus corrupt fixtures/step/canonical fixtures/step/damaged`
from the files above, not from a fresh generation – so they are reproducible
even though generation is not. Each mutation point is asserted to occur
exactly once in its source file before it is touched; the tool fails rather
than damaging whichever occurrence it found first. `DAMAGE-REPORT.txt` records
what was done to each file and at which byte.

Here "damaged" means a controlled violation of the file's syntax or references,
not a promise that every tolerant STEP reader returns an error. A reader may
recover, ignore the affected metadata or produce a partial model. Section 18
must specify and test FerriteCAD's policy for each outcome; parse success alone
must not be mistaken for a sound import. Its gates must collect diagnostics from
both reading and transfer and compare the complete XDE scene – assembly tree,
definitions and instances, names, colours, units and placements – as well as
shape validity and geometry. Equal solids and volume alone do not establish an
equivalent import.

One of these damages an identity rather than a value.
`06-duplicate-product-definition.step` writes `#31` twice with different
contents – the second carries another part's formation and context – so one
identifier names two product definitions while every reference in the file
still resolves. It exists because a definition in an imported assembly is keyed
by that identifier, and until this file the corpus could not say what happens
when the key's own identifier is written twice. `05-duplicate-entity-id.step`
does not ask that question: it duplicates a shape representation, and
duplicates it identically, so nothing about it is ambiguous.

The reproducible observation is made by `tools/step-diagnostic` in the pinned
OCCT workflow. It records load diagnostics even when `ReadFile` refuses the
file, records transfer diagnostics separately, and describes definition
sharing, instance colour overrides and complete local placements. The workflow
compares the three reports after removing only the CR in CRLF line endings;
the raw uploaded artefacts are not rewritten.

| File | Bytes | SHA-256 |
|---|---:|---|
| `01-truncated.step` | 9375 | `3e0d641594314d63fc66b2bbb6d21feaf177a856c5f25851dbf3465277928820` |
| `02-broken-reference.step` | 23534 | `1ff03d8dbb9abff3cf1ac5f545d02a3b394fd8ebafa47cb31d35956a5484941e` |
| `03-missing-terminator.step` | 20063 | `231bfbc2a09d925738a208954b6beb87e3f45245a68a0a1375bb2ce3c1d7d1a5` |
| `04-corrupted-number.step` | 10283 | `3bd6eb2919480afb41041d6f76cc7da78661f50f870f242959b67ad678ed4b68` |
| `05-duplicate-entity-id.step` | 15831 | `e787d181af1d706aab71180ff12baeb59168a7902fbabd442798ba53df43a6bb` |
| `06-duplicate-product-definition.step` | 23577 | `a7d15d7f14c0d558c69ff15c1f0ac70a34151f8016ca39b4705abdc9dd732b2f` |
| `DAMAGE-REPORT.txt` | 1490 | `ac057d314f3b96b6d804eef55e136a39287334005edfdb448331404c2b7967c9` |

## Regenerating

The corpus comes from the pinned-OCCT workflow, which builds the generator
against `V8_0_1`, generates twice, and fails if the two semantic manifests
disagree. Take the Linux artefact from a green run. The damaged variants can
be rebuilt anywhere:

```
step_corpus corrupt fixtures/step/canonical fixtures/step/damaged
```

Expect the STEP checksums to change when the corpus is regenerated and the
manifest not to. If the manifest changes, something about the model changed
and the diff says what.
